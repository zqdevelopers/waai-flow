{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --prefix backend && npm ci --prefix frontend && npx --prefix backend prisma generate && npx --prefix backend prisma db push && npm run build --prefix frontend"
  },
  "deploy": {
    "startCommand": "node backend/src/app.js",
    "healthcheckPath": "/api/status",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
